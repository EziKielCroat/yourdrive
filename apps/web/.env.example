NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# API proxy target (Vite dev server). Default: http://localhost:3000
# Only needed if your API runs on another host (e.g. another machine or IP)
# API_PROXY_TARGET=http://localhost:3000

# Optional: document conversion service (Vert)
VITE_VERT_URL=http://localhost:3003
